```yaml
name: r3al3r_app
description: R3AL3R AI Mobile App
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  encrypt: ^5.0.1
  jwt_decoder: ^0.3.0
  speech_to_text: ^6.1.0
  flame: ^1.4.0
  flutter_tailwindcss: ^0.1.0
  flutter_secure_storage: ^8.0.0
  flutter_local_notifications: ^17.0.0
  web3dart: ^2.7.0
  html: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
```